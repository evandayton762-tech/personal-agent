# Runner Core Types

This document references the task envelope specification defined in `/docs/spec_task_envelope.md`. The runner relies on these types to validate incoming steps and produce `StepResult` objects. Implementation details will follow in later milestones.