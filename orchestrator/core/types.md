# Orchestrator Core Types

This document references the task envelope specification defined in `/docs/spec_task_envelope.md`. The orchestrator uses these types to represent incoming specifications, plans, steps, results, and parked items. See the specification document for details.